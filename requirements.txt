# =============================================================================
# requirements.txt — Python Package Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# Or just run:  make install
#
# Each package is pinned to a minimum version to avoid breaking changes.
# =============================================================================

# --- Web Framework ---
# FastAPI is the web framework. It handles routing, validation, and docs.
fastapi>=0.115.0

# Uvicorn is the ASGI server that actually runs FastAPI.
# The [standard] extra includes useful performance add-ons.
uvicorn[standard]>=0.30.0

# --- Data Validation & Settings ---
# Pydantic v2 validates request/response data and reads .env files.
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- HTTP Client ---
# httpx is like "requests" but supports async (non-blocking) calls.
# Used to talk to Land Registry SPARQL API and x402 facilitator.
httpx>=0.27.0

# --- Redis ---
# Async Redis client for caching and rate limiting.
# The [hiredis] extra makes it faster with a C-based parser.
redis[hiredis]>=5.0.0

# --- Testing ---
# pytest runs the test suite. pytest-asyncio lets us test async functions.
pytest>=8.0.0
pytest-asyncio>=0.23.0

# --- Utilities ---
# python-dotenv loads .env files (used by pydantic-settings).
python-dotenv>=1.0.0
PyJWT>=2.0
cryptography>=41.0
httpx>=0.27.0
